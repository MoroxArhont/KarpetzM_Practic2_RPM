<Window x:Class="KarpetzM_Practic2_RPM.Task5Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Задача 5" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,15"
                   Text="Условие: Сгенерировать массив M×N со значениями от -10 до 10. Отсортировать по возрастанию и по убыванию. Найти min и max."/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="N (строк):" VerticalAlignment="Center" Width="90"/>
            <TextBox x:Name="txtN" Width="60" Height="25" Margin="0,0,20,0"/>
            <TextBlock Text="M (столбцов):" VerticalAlignment="Center" Width="110"/>
            <TextBox x:Name="txtM" Width="60" Height="25"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Сгенерировать и отсортировать" Width="250" Height="35"
                HorizontalAlignment="Left" Margin="0,10,0,10" Click="btnCalculate_Click"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="По возрастанию:"
                       FontWeight="Bold" Margin="5"/>
            <DataGrid Grid.Row="1" Grid.Column="0" x:Name="dgAsc"
                      AutoGenerateColumns="True" IsReadOnly="True"
                      Margin="5" CanUserAddRows="False"/>

            <TextBlock Grid.Row="0" Grid.Column="1" Text="По убыванию:"
                       FontWeight="Bold" Margin="5"/>
            <DataGrid Grid.Row="1" Grid.Column="1" x:Name="dgDesc"
                      AutoGenerateColumns="True" IsReadOnly="True"
                      Margin="5" CanUserAddRows="False"/>
        </Grid>

        <TextBlock Grid.Row="4" x:Name="txtMinMax" FontSize="14" Margin="0,10,0,0"/>

        <Button Grid.Row="5" Content="Закрыть" Width="150" Height="30"
                HorizontalAlignment="Left" Margin="0,10,0,0" Click="btnClose_Click"/>
    </Grid>
</Window>