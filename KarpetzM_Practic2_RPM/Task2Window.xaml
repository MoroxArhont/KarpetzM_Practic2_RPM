<Window x:Class="KarpetzM_Practic2_RPM.Task2Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Задача 2" Height="450" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,15"
                   Text="Условие: Дана строка из русских слов, разделённых пробелами. Вывести эти же слова в обратном порядке, разделённые одним пробелом."/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Введите строку:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtInput" Height="60" TextWrapping="Wrap"
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Перевернуть" Width="150" Height="35"
                HorizontalAlignment="Left" Margin="0,10,0,10" Click="btnCalculate_Click"/>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Результат:" Margin="0,0,0,5"/>
            <TextBox x:Name="txtResult" Height="80" IsReadOnly="True"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Закрыть" Width="150" Height="30"
                HorizontalAlignment="Left" Margin="0,10,0,0" Click="btnClose_Click"/>
    </Grid>
</Window>